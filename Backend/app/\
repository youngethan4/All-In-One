module.exports = function(mysql){

	var con = mysql.createConnection({
		host: "localhost",
		user: "trippyacidcats",
		password: "sammy15GAYER!",
		database: "all_one"
	});

	con.connect(function(err) {
		if(err) throw err;
		console.log("Connected!");
	});

	getCon:  function(){
		return con;
	}
};
